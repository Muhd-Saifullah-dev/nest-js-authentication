services:
  db:
    image: mysql:8
    container_name: nest-js-auth
    ports:
      - "3308:3306"
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: nest_db
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: